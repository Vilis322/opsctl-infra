all:
  children:
    demo:
      hosts:
        demo-vps:
          ansible_host: "{{ lookup('env', 'DEMO_VPS_IP') }}"
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_ed25519_demo
